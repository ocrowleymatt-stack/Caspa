# CASPA Verification Record

## Responsive visual review

The authenticated homepage and account settings were reviewed at **1440 × 1000** and **390 × 844**. The obsidian/deep-slate surfaces, antique-brass actions, ivory typography, serif display hierarchy, compact navigation, and card treatments remain consistent across both breakpoints. The launchpad collapses into a readable single-column flow on mobile, all format controls remain reachable, author metadata remains visible, and destructive settings controls retain their confirmation fields without horizontal overflow.

The Settings page contains only author-facing account, data export, backup, project deletion, and account deletion controls. No environment names, model identifiers, provider messages, deployment status, source-control metadata, or infrastructure diagnostics are rendered.

## Automated validation

The project passes TypeScript validation, a production build, schema-parity generation, and **32 Vitest assertions across 8 test files**. The expanded suite covers manuscript and production-state transitions, production-asset ownership concealment, immutable layout restoration, proof-resolution history and closure rules, illustration suitability, visual-program approval gates, editable continuity requirements, deterministic pagination, proof-version integrity, cover/bleed/safe-area/font/accessibility checks, anonymous production access rejection, raw-error sanitization, and destructive revision-length rejection.

## Authenticated workflow verification

A disposable authenticated manuscript was driven through the real protected procedures from **draft → diagnosed → plan-approved → revision-running → review → export-ready → archived**. Diagnosis safely used the deterministic evidence-backed fallback when the upstream response was not valid structured JSON. The chapter revision produced an unexpectedly short rewrite; CASPA’s server-side preservation guard rejected it, retained the source checkpoint, recorded a warning, and completed as `succeeded-with-warnings`. All four export preflight checks then passed, an authorized Markdown download was generated, the project was archived, and the disposable data was deleted.

Every project workspace was visually captured at desktop width using an export-ready fixture: overview, manuscript, diagnosis, revision, versions, and export. Manuscript and diagnosis history were then tightened to read-only outside their canonical states and rechecked on mobile. Authenticated mobile captures additionally covered the launchpad, account Settings, completed revision, version comparison and guarded restore, and export preflight/download state. Both temporary visual fixtures were removed after verification.

## Book-production verification

A disposable authenticated illustrated book was driven through the complete protected workflow: **draft → diagnosed → plan-approved → revision-running → review → export-ready → art-direction → art-approved → layout → proof-review → production-ready → archived**. The run created and approved a versioned visual brief, a 1,800 × 2,700 print-normalized cover, an author-revised 5:4 illustration brief, a saved continuity profile, an approved print-normalized illustration, a 15-page immutable layout, a separately approved passing proof, and four versioned output artifacts.

Production preflight passed all twelve server checks: approved cover, cover resolution, illustration availability, illustration alt text, bleed, text safe areas, font policy, title/author metadata, pagination and overflow, intentional blank pages, resolved proof comments, and latest submitted proof. The generated outputs were an interior PDF, cover PDF, reflowable EPUB, and source package with manifests and checksums. The workflow then archived and deleted its disposable data.

Desktop captures covered visual direction, cover comparison, illustration continuity and approval, layout composition, proof review, and final editions at **1440 × 1000**. Mobile captures covered cover, illustrated-asset review, proof checks, and final editions at **390 × 844**. A dense mobile workflow rail was replaced with a compact current-folio treatment; production tabs remain horizontally scrollable without label collision. The visual language was refined to use quiet slate for completed steps and brass only for active decisions and approval marks.

The final layout and proof captures explicitly verified **75–150% zoom controls**, a toggleable visible text-safe-area overlay, and timestamped proof history showing the original comment, its resolved state, resolution time, and author-visible resolution note. The same illustrated fixture was then archived through the protected project transition and deleted through the guarded confirmation flow. Final checks completed with no schema drift.

## Draft with CASPA verification

An initial desktop capture exposed an invalid standalone radio item in the new drafting mode selector. The selector was corrected to use a controlled radio group, restoring the workspace without a client exception. Authenticated project, active-version, and protected draft-preview requests then completed successfully for a preserved fixture containing a pending, continuity-grounded “After the Bell” chapter preview.

The repaired draft workspace was captured at **1440 × 1000** and **390 × 844**. Both breakpoints show the same visible guided entry point, three drafting scopes, premise/outline/voice/exclusion controls, an independently scrollable private preview, copy/reject/regenerate paths, and a disabled-until-confirmed acceptance action. The published manuscript remains visibly separate from the preview, with version count and active word count retained in the adjacent project context. The mobile layout retains readable labels, reachable controls, and a vertically coherent preview-to-acceptance path without horizontal overflow.

The authenticated launchpad was then captured at **1440 × 1000** and **390 × 844**. It now names Draft with CASPA before creation, explains its approval-gated purpose, and makes “Create project & draft with CASPA” the primary action. On mobile, the format options, premise, metadata fields, target words, and direct drafting CTA remain readable and reachable in a single vertical flow.

An accepted auto-draft fixture was captured in Version History at **1440 × 1000** and **390 × 844**. The interface visibly distinguishes the empty `Project created` version from the active `CASPA draft · The Missing Shelf` version, shows the +301-word and +1-section delta, renders their side-by-side contents, retains source-version provenance, and offers only the guarded “Restore as new snapshot” path. The mobile comparison preserves these controls and its active-version status without horizontal overflow.

The Draft with CASPA release validation completed with **44 passing Vitest assertions across 10 files**, a clean TypeScript check, successful production build, and no outstanding Drizzle schema changes. The protected end-to-end run confirmed non-mutating rejection, immutable accepted-version creation, grounded next-chapter preview, and Workshop diagnosis handoff. All temporary draft fixtures were archived and deleted after visual review.

The final strict-evidence run completed with **48 passing Vitest assertions across 11 files**, including four protected real-database preview lifecycle cases. The cases covered rejection without active-version mutation, accepted-version persistence, stale-source refusal, and cross-user denial. A database cleanup query immediately after the full suite returned zero disposable integration projects and zero temporary integration users.

The same 300-word literary-suspense opening scenario was then posted directly to the isolated incumbent auto-write API. The incumbent returned HTTP 500 without a chapter and included internal provider/availability diagnostics plus a masked credential fragment; the raw response was immediately deleted. The rebuilt protected path had already completed the same scenario with an accepted 301-word source-linked version and a grounded next-chapter preview. The direct comparison is recorded in `INCUMBENT_AUTO_WRITE_COMPARISON.md` and deliberately limits the claim to measurable operational behavior, not subjective prose superiority.

## Consent-first style and collaboration verification

The private Style Library, direct collaborator invitation route, project Collaboration Desk, Draft with CASPA profile affordance, and role-scoped approval guidance were reviewed at **1280 × 900** and **390 × 844**. The mobile layouts retain readable consent language, source-note controls, profile selection, invitation code entry, role choice, approval gates, blind-review setup, and audit-history hierarchy without horizontal overflow. The unavailable approval state now explains that an author configures gates while only accepted editors and designers receive assigned decisions.

The collaboration persistence suite exercised a real database: an owner invited an editor, the invited email accepted, an outsider was denied the blind review, the editor received an anonymous manuscript/reviewer label, one sealed review was recorded, a second submission was rejected, revocation immediately removed reviewer access, and every resulting event appeared in the owner audit trail. The suite also rejects email-mismatched invitation acceptance and fabricated approval target identifiers. Private style tests confirm consent enforcement, owner-only active-profile retrieval/status changes, and author-only removal of an unprofiled sample. The final run completed with **55 passing assertions across 14 files**; a cleanup query confirmed zero temporary collaborator users, style users, and visual fixtures.

The completed collaboration release now validates **59 passing assertions across 14 files**. Blind review assigns a randomized non-identifying manuscript folio and a distinct randomized reader label per assignment; tests prove labels differ across rounds and contributor identity remains absent from reviewer payloads. The author Collaboration Desk provides reviewer selection, aggregate scores, close-round controls, permitted closed-round feedback, immutable editor/designer decisions, and a project audit ledger.

A complete disposable authenticated run selected a consented private craft profile for Draft with CASPA, accepted the resulting version, invited and authenticated an editor, completed and closed an anonymous review, released permitted feedback, configured and recorded a real editor revision approval, revoked editor access, exported both sanitized style metadata and the owner account audit data, and then removed every project, collaborator, review, decision, profile, sample, and audit fixture. A post-run database query returned zero matching records. Final mobile captures confirm the source upload/export, invitation, approval guidance, and unavailable-review recovery layouts remain readable and no longer trap missing/revoked reviewers in a loader.
