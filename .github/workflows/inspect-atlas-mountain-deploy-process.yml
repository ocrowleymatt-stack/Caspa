name: Inspect Atlas Mountain deploy process

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/inspect-atlas-mountain-deploy-process.yml'

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      ATLAS_HOST: 116.202.24.63
      PRIVATEKEY2: ${{ secrets.PRIVATEKEY2 }}
      HETZNER_SSH_KEY: ${{ secrets.HETZNER_SSH_KEY }}
      ATLAS_SSH_PRIVATE_KEY: ${{ secrets.ATLAS_SSH_PRIVATE_KEY }}
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
      SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
    steps:
      - name: Inspect live authentication routing
        shell: bash
        run: |
          set -euo pipefail
          key=''
          for name in PRIVATEKEY2 HETZNER_SSH_KEY ATLAS_SSH_PRIVATE_KEY SSH_PRIVATE_KEY DEPLOY_SSH_KEY SERVER_SSH_KEY; do
            if [ -n "${!name:-}" ]; then key="${!name}"; break; fi
          done
          test -n "$key"
          mkdir -p ~/.ssh
          printf '%s\n' "$key" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -T 10 -H "$ATLAS_HOST" >> ~/.ssh/known_hosts
          ssh -o BatchMode=yes -o ConnectTimeout=20 -i ~/.ssh/id_ed25519 root@"$ATLAS_HOST" 'bash -s' <<'REMOTE'
          set -u
          echo 'relevant_nginx_configuration:'
          nginx -T 2>/dev/null | grep -n -E -C 8 'atlas\.ocrowley\.com|auth_request|127\.0\.0\.1:3002|/api/v1/auths|oauth|sso' | head -n 600 || true
          echo 'openwebui_auth_probe:'
          status="$(curl -sS -o /tmp/atlas-auth-probe -w '%{http_code}' --max-time 5 http://127.0.0.1:3002/api/v1/auths/ || true)"
          echo "status=$status"
          python3 - <<'PY'
import json
p='/tmp/atlas-auth-probe'
try:
    data=json.load(open(p))
    if isinstance(data, dict): print('body=dict:' + ','.join(sorted(data.keys())))
    elif isinstance(data, list): print('body=list:length=' + str(len(data)))
    else: print('body=json:' + type(data).__name__)
except Exception:
    print('body=non_json_or_empty')
PY
          rm -f /tmp/atlas-auth-probe
          REMOTE
      - name: Cleanup
        if: always()
        run: rm -f ~/.ssh/id_ed25519
