name: Router God Live Smoke

on:
  push:
    paths:
      - '.github/workflows/router-god-live-smoke.yml'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Exercise live Atlas router
        run: node scripts/router-god-live-smoke.mjs
