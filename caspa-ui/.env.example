# Leave unset (or empty) when UI is served from the Express app (production / npm run dev).
# VITE_API_URL=

# Split dev only: Vite on :5173, API on :3000 — uncomment if proxy is insufficient:
# VITE_API_URL=http://localhost:3000
