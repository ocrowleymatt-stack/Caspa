{
  "name": "atlas-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --target=node22 --format=cjs --sourcemap --outfile=dist/atlas-router.cjs",
    "check": "npm run build && node --check dist/atlas-router.cjs"
  },
  "dependencies": { "@google/genai": "^1.50.1" },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "esbuild": "^0.28.0",
    "typescript": "~5.8.2"
  }
}
